# 更新日志

本项目所有值得注意的变更都会记录在此文件。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

> 面向开发者的详细变更（数据结构、内部约定、迁移说明）见 [`docs/UPGRADE.md`](docs/UPGRADE.md)。

## [1.0.2] — 2026-08-31（含 r1–r10 修订）

### 新增

**播放器**
- **长按右方向键临时 2× 加速**：按住 `→` ≥ 0.4 秒进入当前倍速 ×2（上限 6×），松开立即恢复；短按仍是「下一个视频」
- **进度条悬停实时帧预览**：鼠标悬停时间轴即显示该时刻画面
- **外挂字幕**：同目录同名 `.srt` / `.vtt` 自动加载，支持手动选择、开关与字号调节
- **当前帧截图**：`S` 键或控制栏相机按钮，保存为原分辨率 PNG，目录可自定义
- **倍速扩展至 0.25×–6.0×**：预设档位 + 2–6× 自定义滑动条
- 自定义控制栏（单行居中）、快进 / 快退 10 秒、点击画面暂停 / 继续、画中画
- 底部缩略图条支持**滚轮前后浏览**

**浏览与组织**
- **智能归类**：按拍摄日期生成「年 / 年-月」归类计划（含视频），预览后可应用 / 撤销
- **照片时间轴**：按 EXIF / 创建日期分组浏览照片与视频
- **彩色标签全局浏览**与文件夹标签继承
- **媒体详情面板**：视频码流信息与图片 EXIF（相机、镜头、光圈、快门、ISO、GPS）

**维护与设置**
- **缓存目录一键进入**：设置中显示当前资料库缓存目录，点击在 Finder / 资源管理器打开
- **一键清除缓存**与**优化数据库**（VACUUM 压缩，显示释放空间）
- **回收站到期自动清除**（默认 3 天，可设 0–365 天），条目显示剩余天数
- **设置全局化**：所有设置存于应用数据目录，切换资料库不再重置
- 自定义输入框收窄居中，缓存保留时长支持自定义小时数

### 优化

- **性能**：缩略图与预览缓存 WebP 化（体积 −40%）、目录列表 3 秒内存缓存、缩略图线程按 CPU 核数自适应、增量索引扫描（二次扫描秒级）、空闲预生成缩略图、弹窗组件代码分割（主包 −33%）、大图降采样预览（内存 −90%）、超大目录虚拟滚动
- **视频处理主路径改走 AVFoundation**（macOS 系统解码器），失败自动回退 ffmpeg
- 发布包移除 `ffprobe`，体积减少约 49MB
- 缓存按时间自动过期（1 / 6 / 24 小时或永不，支持自定义）
- 转封装缓存「关闭即删」，不占用额外磁盘空间
- 播放器切换 / 关闭时及时释放解码器资源

### 修复

- 首次打开被 Gatekeeper 拦截：补充 ad-hoc 重新签名（`codesign --force --deep --sign -`）
- 版本号 4 处不同步导致应用内显示旧版本
- 图片查看器连点控件被误判双击而误切全屏
- 全屏窗口切换条目后退出，主窗口仍停在切换前的条目
- 缩略图条滚轮前后浏览功能在控制栏重构中丢失后恢复
- 分栏拖拽失效（`:style` 绑定字符串型 CSS 属性时重复包裹对象键）
- 右键菜单渲染崩溃（Vue 模板内使用 TS 断言与多条内联语句）
- 收藏与彩色标签在删除 / 移动 / 恢复后不同步
- 回收站列表缩略图不显示（字段名不匹配）

## [1.0.1] — 2026-08-29（含 r2–r8 修订）

### 新增

- **分栏视图**：目录按 文件夹｜视频｜图片 自动分栏，栏宽可拖动、每栏独立视图与排序
- **全盘模糊搜索**：支持文件名片段、拼音全拼与拼音首字母
- **回收站**：可恢复 / 彻底删除 / 清空，支持多选
- **最近添加**、**收藏夹分类管理**、**文件夹占用显示**
- **右侧批量操作**：批量选中、移动、删除、标记颜色、收藏
- **外部变更自动感知**：文件监听 + 轮询兜底
- **日夜主题**切换、**TMDB 刮削**（可选联网）
- 播放器铺满、视频图片格式扩展（f4v / ogv / asf / divx / mxf / avifs 等）
- 管理操作后自动刷新文件修改时间，排序即时反映

### 修复

- Tauri capabilities 权限缺失导致上传 / 选图对话框失效
- 新建文件夹改自定义弹窗（WKWebView 不支持 `prompt`）
- 右键菜单渲染崩溃、右键误改选中状态
- 文件夹右键菜单失效（组件重写时漏 import）
- 分栏栏宽比例、拖拽 1:1 跟手与最小宽度保护
- 生产包错误调试面板移除
- 搜索 / 最近添加中「查看大图」套用错误图集

## [1.0.0] — 2026-08-28

- 首个可用版本：目录树 + 封面网格浏览、视频播放、图片查看、搜索、重命名与删除
