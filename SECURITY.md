# 安全策略

## 支持的版本

| 版本 | 是否支持 |
|---|---|
| 1.0.2 | ✅ |
| 1.0.1 | ⚠️ 建议升级 |
| < 1.0 | ❌ |

## 安全模型

VTManager 是一款**纯本地运行的桌面应用**，理解这一点有助于判断风险边界：

- **无服务端**：应用不部署任何后端服务，不收集、不上传任何用户数据
- **数据不出本机**：索引数据库、缩略图缓存、应用设置全部存放在**用户本地**（资料库根目录的 `.VTManager/` 与应用数据目录的 `prefs.json`）
- **默认不联网**：除用户主动使用 TMDB 刮削功能（需自行在设置中填写 API Key）外，应用不发起任何网络请求
- **直接操作真实文件**：重命名、移动、删除、智能归类等操作会真实改动磁盘内容；其中删除会先进入回收站，可随时恢复

### 需要用户注意的两点

1. **TMDB API Key 本地存储**：填写的 API Key 以明文形式保存在应用数据目录的 `prefs.json` 中。请勿在不受信任的设备上填写，也不要分享该文件。
2. **本地资源访问范围**：为加载本地图片与视频，应用启用了 Tauri 的 `assetProtocol`，其 scope 配置为 `allow: ["**"]`（`requireLiteralLeadingDot: false`）。这意味着前端可加载任意本地路径资源——**请勿在应用内加载来源不可信的内容**。

### 应用签名

macOS 发布包使用 **ad-hoc 自签名**、未经 Apple 公证，因此首次启动时 Gatekeeper 会拦截，需右键 → 打开。
如需验证下载完整性，请比对 Release 页面提供的 SHA-256 校验值。

## 报告漏洞

如发现安全问题，**请不要直接开公开 Issue**，请通过以下方式私密报告：

- 使用 GitHub 的 [Security Advisories](https://github.com/QuinnLSY/VTManager/security/advisories/new) 私密报告漏洞

报告时请尽量包含：

- 问题类型与影响范围
- 复现步骤（系统版本、应用版本、操作路径）
- 可能的利用方式与危害评估
- 如有可能，附上修复建议

## 响应预期

- 我们将在 **7 天内**确认收到报告
- 确认问题后会在合理时间内给出修复计划，修复发布后会在 Release Notes 中致谢报告者（除非你要求匿名）
- 若问题被判定为不在范围内（如第三方依赖的已知问题），我们也会说明原因
